# Core training
torch>=2.4.0
transformers>=4.46.0
datasets>=2.14.0
deepspeed>=0.15.0
accelerate
packaging
tqdm
sentencepiece

# Optional (recommended for Qwen/Llama speed):
#   pip install flash-attn --no-build-isolation
